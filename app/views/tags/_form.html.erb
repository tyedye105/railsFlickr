<%= form_for [@image, @tag] do |f| %>
  <%= f.select :user_id do %>
    <% @users.each do |user| %>
      <%= options_for_select([[user.username, user.id]]) %>
    <% end %>
  <% end %>
  <%= f.submit "Tag!", :class => 'btn btn-default' %>
<% end %>
